import { CategorySidebar, ProductList } from "../../components";

const ProductListContainer = () => {
  return (
    <div className="page">
      <CategorySidebar />
      <ProductList />
    </div>
  );
};

export default ProductListContainer;
